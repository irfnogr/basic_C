#include <stdio.h> 
int main()
{

int number,temp,a,sum=0;

printf("enter a number: ");
scanf("%d", &number);

temp=number;

while(n>0)
{

  r=number%10;
  sum+=r*r*r;

  number/=10;
}

if(sum==temp)
printf(" the given number %d is a amstrong number",number);
else
printf(" the given number %d is not a amstrong number",number);
}
