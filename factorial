#include <stdio.h> 
int main()
{

int number,fact=1,i;
printf("enter a number: ");
scanf("%d", &number);

for(i=0;i<=number;i++)
{
  fact*=i ;
}

printf("the factorial of %d is: %d", number,fact);

return 0;
}
